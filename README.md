# GitHub Stars Dashboard

A static GitHub Pages dashboard for BoscoCJ's starred repositories.

- `index.html` is the published dashboard.
- The page is generated locally from `github-stars/build_catalog.py`.
